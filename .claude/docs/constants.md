# 常量定义 (utils/constant.js)

```javascript
import CONSTANT from '@/utils/constant'
```

## 基础常量

```javascript
FILE_MAX_SIZE = 20         // MB
IMAGE_MAX_SIZE = 5         // MB
INFO_MAX_LENGTH = 50       // 名称/编码最大长度
REMARKS_MAX_LENGTH = 1000  // 备注最大长度
```

## 审核状态

```javascript
AUDIT_STATUS: {
  SAVE: -1,     SAVENAME: '待提交',
  SUBMIT: 0,    SUBMITNAME: '待审核',
  PASS: 1,      PASSNAME: '审核通过',
  NOTPASS: 2,   NOTPASSNAME: '审核不通过',
  BACK: 3,      BACKNAME: '审核退回'
}
```

## 角色表 ROLE_TABLE（基于 sys_role.id）

```javascript
ROLE_TABLE: {
  SUPER_ADMIN: 1,
  SCHOOL_TEACHER: 2,
  STUDENT: 3,
  DEPARTMENT_ADMIN: 13,
  ACADEMIC_AFFAIRS_ADMIN: 14,
  SCHOOL_ADMIN: 15,
  COMPANY_TUTOR: 18,
  COMPANY_ADMIN: 19,
}
```

## 岗位类型 USER_JOB_CODE（基于 base_job_position.jobCode）

```javascript
USER_JOB_CODE: {
  SUPER_ADMIN: 'SUPER_ADMIN',
  STUDENT: 'STUDENT',
  COMPANY_TUTOR: 'COMPANY_TUTOR',
  COMPANY_ADMIN: 'COMPANY_ADMIN',
  SCHOOL_TEACHER: 'SCHOOL_TEACHER',
  SCHOOL_ADMIN: 'SCHOOL_ADMIN',
  DEPARTMENT_ADMIN: 'DEPARTMENT_ADMIN',
  ACADEMIC_AFFAIRS_ADMIN: 'ACADEMIC_AFFAIRS_ADMIN',
}
```

> **注意**: `ROLE_TABLE` 和 `USER_JOB_CODE` 含义不同，前者是角色 ID，后者是岗位编码字符串，勿混用。

## 实习流程类型 PROCESS_TYPE

```javascript
PROCESS_TYPE: {
  INTERNSHIP_PLAN_MAKE: 'INTERNSHIP_PLAN_MAKE',
  STUDENT_SELECT_INTERNSHIP: 'STUDENT_SELECT_INTERNSHIP',
  TEACHER_SELECT_INTERNALSHIP: 'TEACHER_SELECT_INTERNALSHIP',
  INTERNAL_TEACHER_SELECT_PROJECT: 'INTERNAL_TEACHER_SELECT_PROJECT',
  INTERNAL_TEACHER_DECLARE_TOPIC: 'INTERNAL_TEACHER_DECLARE_TOPIC',
  INTERNAL_STUDENT_TEACHER_MATCH: 'INTERNAL_STUDENT_TEACHER_MATCH',
  EXTERNAL_ENTERPRISE_POST_DECLARATION: 'EXTERNAL_ENTERPRISE_POST_DECLARATION',
  EXTERNAL_STUDENT_SELECT_POST: 'EXTERNAL_STUDENT_SELECT_POST',
  EXTERNAL_STUDENT_SELF_DECLARATION: 'EXTERNAL_STUDENT_SELF_DECLARATION',
  EXTERNAL_STUDENT_ASSIGN_POST: 'EXTERNAL_STUDENT_ASSIGN_POST',
  EXTERNAL_ASSIGN_INTERNAL_TUTOR: 'EXTERNAL_ASSIGN_INTERNAL_TUTOR',
  EXTERNAL_ENTERPRISE_ASSIGN_TUTOR: 'EXTERNAL_ENTERPRISE_ASSIGN_TUTOR',
  STUDENT_INTERNSHIP_TERMINATION: 'STUDENT_INTERNSHIP_TERMINATION', // 终止学生实习
}
```

## 审核级别 VERIFY_LEVEL

```javascript
VERIFY_LEVEL: {
  NO_VERIFY: 1,
  ONE_VERIFY: 2,
  TWO_VERIFYS: 3,
  THREE_VERIFYS: 4,
  FOUR_VERIFYS: 5,
  FIVE_VERIFYS: 6
}
```

## 搜索操作符 SEARCH_OPERATOR

```javascript
SEARCH_OPERATOR: {
  EQ: '=', NE: '!=', LIKE: '≈',
  IN: '()', NOT_IN: '!()',
  GT: '>', LT: '<', GE: '>=', LE: '<=',
  RANGE: '<=>', OR: '||', AND: '&&',
  FIND_IN_SET: 'fi()'   // MySQL FIND_IN_SET，用于逗号分隔列表的包含查询
}
```

## 文件类型 FILE_TYPE

```javascript
FILE_TYPE: {
  PDF: ['pdf'],
  DOC: ['doc', 'docx'],
  IMG: ['png', 'jpg', 'jpeg', 'gif'],
}
```

## 自主实习虚拟岗位 SELF_INTERNSHIP

```javascript
SELF_INTERNSHIP: {
  POST_CODE: 'SELF_INTERNSHIP',  // MainInternshipPost 中识别虚拟岗位的 code
  POST_NAME: '自主实习',
  UNLIMITED: -1,                  // allPersonNum=-1 表示无人数限制
}
```

> 用法：校外项目创建时后端自动建一条 code=`SELF_INTERNSHIP` 的虚拟岗位；学生申报数据以 `self_*` 字段写入 `RelStuInternshipPost`，附件 `tableName='RelStuInternshipPost'`。详见 `internship-process.md`。
